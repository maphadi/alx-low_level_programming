#include "main.h"

/**
 *Write a function that multiplies two integers
 *int mul(int a, int b)
 * Return: Always 0.
 */

int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	return (1);
	else
	return (0);
}
