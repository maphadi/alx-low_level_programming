#include <stdio.h>

/**
 * main - Determines if a number is positive, negative or zero.
 *
 * Return: Always 0 (Success)
 */


int main (void)
{
	unsigned char n;
	n=0;
	while (n<10)
	{
	putchar("%d", n);
	n++;

	return(0);
}

